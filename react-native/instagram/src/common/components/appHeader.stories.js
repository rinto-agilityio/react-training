// Libs
import React from 'react'
import { storiesOf } from '@storybook/react-native'

// Components
import AppHeader from './AppHeader'

storiesOf('Layout', module).add('AppHeader', () => <AppHeader />)
